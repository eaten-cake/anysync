# AGENTS.md

本文件约束在本仓库工作的 AI 编码助手。与用户交流请用中文。

## 项目定位

AnySync：基于 WebDAV 的极简多端文件同步 CLI（Rust）。

- 用法规格以 `docs/design/usage.md` 为准；README 面向使用者，行为变化时同步更新。
- 当前支持配置选择 `alist` 或 `webdav`，共用 WebDAV 传输；AList 自动使用服务地址的 origin + `/dav`，标准 WebDAV 手动指定 endpoint。新增其他后端由用户决定，不要提前实现。

## 代码风格（最高优先级）

- **极简优先**：只实现当前需要的功能。未经用户要求，不得引入 trait 抽象层、插件机制、状态数据库、异步运行时等复杂结构。
- **模块职责明确**：`main.rs`（入口分发）、`cli.rs`（命令定义）、`config.rs`（配置与仓库查找）、`sync.rs`（扫描/计划/执行）；`backend/mod.rs` 定义公共接口、文件信息和后端构造，`backend/alist.rs` 封装 AList 差异，`backend/webdav.rs` 实现标准 WebDAV。同步层仅依赖公共后端接口，AList 复用 WebDAV 协议代码。新增逻辑先进现有模块，进一步拆分先和用户讨论。
- **依赖从简**：每加一个依赖都要有明确理由，优先复用已有依赖与标准库。
- 错误处理统一用 anyhow（`bail!`/`context`）；面向用户的提示用中文，并包含实际路径与 HTTP 状态码。
- 注释只写代码本身表达不了的约束（如"405 表示已存在"），不写"这行在做什么"。

## 同步行为约定（改动前必读）

- 新旧判断：文件 mtime 比较，容差 2 秒（`TOLERANCE`）；较新的一方永不被旧的一方覆盖。
- 修改时间相同但大小不同 → 冲突：跳过并提示，禁止静默覆盖。
- 删除不传播；空目录、`.anysync/`、`.git/` 不同步；符号链接不跟随。
- 下载必须先写临时文件再原子替换，并保留远端 mtime（防止 push/pull 回环重复传输）。
- 使用 `init`、`config` 后自行 `pull`/`push`，不提供 clone。配置显式保存 backend；旧配置缺少 backend 时按 webdav 处理。root 相对于 endpoint，禁止猜测或重复添加入口路径。
- 上传完成后用一次全量 `PROPFIND` 回读可见文件的 mtime 并写回本地，防止 push/pull 回环。三条守卫下跳过对齐——远端 mtime 为 0、远端大小与本地不符（远端可能已被第三方覆盖，强行对齐会让对方的新内容再也拉不下来）、本地文件在 push 期间被改动。对齐失败降级为告警，不影响退出码；远端暂不可见的文件不重传、不报错，后续 push/pull 自愈。
- push 前按路径排序创建远端目录（父目录天然在前）；远端根目录 MKCOL 尽力而为（忽略 405）。
- 密码：不作为命令行参数、不回显；交互终端无回显输入，非交互环境从 stdin 读一行。

## 验证要求

交付前必须全部通过：

1. `cargo fmt`
2. `cargo clippy`
3. `cargo test` —— `plan()` 等纯逻辑必须有单元测试；改动比较规则时先补测试再改代码。

## 安全

`.anysync/config.toml` 中的密码是明文。该目录已加入 `.gitignore`，绝不提交、绝不同步、绝不在命令输出中打印其内容。
